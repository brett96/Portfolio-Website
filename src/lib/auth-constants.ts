/**
 * Shared auth constants. Used by middleware (cookie check), admin layout (verification),
 * and Server Actions (set/clear cookie). Must stay in sync.
 */
export const SESSION_COOKIE_NAME = "session";
